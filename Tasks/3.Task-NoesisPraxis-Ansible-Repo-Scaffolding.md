Task:
Generate the bootstrap files for the NoesisPraxis Ansible repository:
1. `README.md`
2. `ansible.cfg`
3. `requirements.yml`

These files must align with the repository skeleton and orchestration plan already defined for the NoesisPraxis multi-agent stack. They must be immediately usable and should establish the repo’s operator experience, Ansible runtime defaults, and collection dependencies.

General constraints:
- Do not create any other files in this task.
- Do not implement playbook logic here.
- Do not invent alternate repo layout or filenames.
- Keep the content consistent with the NoesisPraxis stack naming.
- The bootstrap must support local, macOS, Tailscale, registry, security, Telegram, OpenClaw, and Hermes workflows.
- The files should be ready for direct commit into the repository.

File 1 — `README.md`
Purpose:
Provide a concise operator-facing overview of the repo.

Required content:
- Project name: NoesisPraxis Ansible Stack.
- Short description of the repository purpose.
- Explain that the repo manages a multi-agent system with:
  - ACP registry
  - ANS
  - Agent Registry dashboard
  - A2A registry
  - MCPJungle
  - Clawvisor
  - Telegram communications
  - OpenClaw and Hermes runtimes
  - macOS ClawDev and HermesDev
  - Tailscale management
  - backup / restore / validation / rollback
- Explain the repo’s role as the Ansible source of truth.
- Include a compact directory overview referencing:
  - `inventory/`
  - `playbooks/`
  - `roles/`
  - `templates/`
  - `files/`
  - `scripts/`
- Explain the intended orchestration flow:
  - bootstrap
  - identity/registry foundation
  - security/tooling
  - runtime deployment
  - macOS and Tailscale
  - Telegram communications
  - validation and maintenance
- Include a short “How to run” section with example commands for:
  - `ansible-playbook playbooks/site.yml`
  - `ansible-playbook playbooks/master-stack.yml`
  - `ansible-playbook playbooks/validate.yml`
- Include a short “Inventory and environments” section describing:
  - `inventory/local`
  - `inventory/tailscale`
  - `inventory/production`
- Include a short “Security and secrets” section noting:
  - secrets live in vault or equivalent protected storage
  - no hardcoded secrets in templates
  - least privilege and local-first defaults
- Include a short “Mac-specific notes” section noting:
  - launchd-based services
  - Apple Silicon compatibility
  - localhost and Tailscale accessibility
  - conservative CPU/RAM budgets
- Keep the README readable and compact, with headings and bullets.

File 2 — `ansible.cfg`
Purpose:
Set safe repository-local defaults for Ansible.

Required content:
- Use repository-local inventory by default, but make it easy to override.
- Set a sensible roles path that includes `roles/`.
- Set a sensible collections path.
- Enable YAML callback output if appropriate.
- Set retry files behavior to off or controlled.
- Keep host key checking and privilege settings aligned with secure defaults.
- Use sane defaults for forks, gathering, and fact caching suitable for a small home-lab stack.
- Make the config compatible with:
  - local development
  - macOS control node use
  - remote Tailscale access to the target Mac
- Include comments only where useful.
- Keep the file minimal and practical, not bloated.

Suggested baseline behavior:
- `inventory = inventory/local/hosts.ini` or equivalent repo-local default.
- `roles_path = roles`
- `collections_paths = ~/.ansible/collections:/usr/share/ansible/collections`
- `stdout_callback = yaml` if available and practical.
- `retry_files_enabled = False`
- `host_key_checking = True`
- `interpreter_python = auto_silent` or equivalent safe default if appropriate.
- `forks` should be conservative.
- Use a local temp/cache path under `.ansible/` or `~/.ansible/` if needed.

File 3 — `requirements.yml`
Purpose:
Declare Ansible Galaxy collections and roles required by the repo.

Required content:
- Include the minimal collection dependencies needed for this stack scaffold.
- Prefer widely used, stable collections suitable for:
  - Dockerized deployments
  - macOS service management where supported
  - Tailscale or SSH-based connectivity workflows
  - YAML and JSON templating/validation support
- Include a brief comment section if needed to group dependencies by purpose.
- Keep dependencies realistic and minimal for the scaffold.
- Do not over-specify unrelated collections.

Dependency categories to consider:
- container and docker support
- common utilities
- JSON/schema helpers if needed
- macOS-related helpers if needed
- any collection required by the planned implementation style

Output requirements:
- Output each file in its own fenced code block.
- Precede each code block with the full file path.
- Do not output any additional files.
- Ensure the files are directly usable as repo bootstrap content.

Acceptance criteria:
- `README.md` clearly explains the purpose and structure of the repository.
- `ansible.cfg` provides safe repo-local defaults.
- `requirements.yml` lists the dependencies needed to start implementation.
- The three files together let the repo bootstrap cleanly for future playbook and role generation.

Now generate:
1. `README.md`
2. `ansible.cfg`
3. `requirements.yml`
