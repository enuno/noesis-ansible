Task:
Generate the shared template and file scaffolding for the NoesisPraxis Ansible repository exactly as specified below. Do not rename paths, do not add extra files, and do not collapse directories. Create the shared templates, schema files, and utility scripts so the repository is ready for later role implementation and playbook execution.

Repository paths to generate exactly:

ansible/templates/
├── common/
│   ├── docker-compose.yml.j2
│   ├── env.j2
│   ├── healthcheck.sh.j2
│   └── backup-manifest.yml.j2
├── registries/
│   ├── acp-agent.json.j2
│   ├── ans-agent.json.j2
│   ├── registry-agent.json.j2
│   ├── a2a-agent-card.json.j2
│   ├── mcp-server.json.j2
│   └── telegram-agent.json.j2
├── macos/
│   ├── launchd-plist.j2
│   ├── resource-budget.yml.j2
│   └── local-services.yml.j2
└── security/
    ├── clawvisor-policy.yml.j2
    └── acl.yml.j2

ansible/files/
├── schemas/
│   ├── acp-registry.schema.json
│   ├── ans.schema.json
│   ├── agentregistry.schema.json
│   ├── a2a-agent-card.schema.json
│   ├── mcp-server.schema.json
│   └── telegram-agent.schema.json
└── scripts/
    ├── validate-json.sh
    └── health-check.sh

General requirements:
- Create exactly the files above.
- Do not add extra files.
- Do not rename any file or directory.
- Keep all files valid in their target format.
- Keep the content minimal but useful.
- Templates must be UTF-8 text files and suitable for Ansible rendering.
- JSON files must be syntactically valid JSON.
- YAML files must be syntactically valid YAML.
- Shell scripts must be executable-safe stubs with a proper shebang.
- Use placeholder variables and comments only where they clarify intent.

Shared template requirements:

1. `templates/common/docker-compose.yml.j2`
- Provide a generic docker-compose template for agent and registry services.
- Include service name placeholders, image placeholders, volumes, environment, ports, restart policy, and healthcheck hooks.
- Keep it reusable for OpenClaw, Hermes, registries, and support services.

2. `templates/common/env.j2`
- Provide environment variable lines only.
- Include placeholders for registry endpoints, hostnames, tokens, binds, and runtime settings.
- Keep secrets referenced indirectly, not embedded.

3. `templates/common/healthcheck.sh.j2`
- Provide a simple shell healthcheck template.
- Include placeholders for URL, process check, or TCP probe logic.
- Make it safe and generic.

4. `templates/common/backup-manifest.yml.j2`
- Provide a manifest template listing paths, labels, timestamps, and included components.
- Support backup and restore workflows across registries, configs, and runtime state.

5. `templates/registries/acp-agent.json.j2`
- Provide a valid ACP registry entry template.
- Include agent identity, lifecycle, endpoints, capabilities, timestamps, and metadata.
- Keep it machine-readable and schema-friendly.

6. `templates/registries/ans-agent.json.j2`
- Provide a valid ANS identity template.
- Include naming, resolution, certificate, and protocol metadata placeholders.

7. `templates/registries/registry-agent.json.j2`
- Provide a generic agent registry entry template.
- Include dashboard-facing metadata, roles, status, and source references.

8. `templates/registries/a2a-agent-card.json.j2`
- Provide a valid A2A Agent Card template.
- Include endpoints, capabilities, authentication hints, and schema-compatible fields.

9. `templates/registries/mcp-server.json.j2`
- Provide a valid MCP server registration template.
- Include server identity, transport, tool groups, allowlists, and policy hints.

10. `templates/registries/telegram-agent.json.j2`
- Provide a valid Telegram agent/bot metadata template.
- Include bot identity, group chat metadata, transport mode, and lifecycle state.
- Do not embed secrets.

11. `templates/macos/launchd-plist.j2`
- Provide a generic launchd plist template.
- Include ProgramArguments, KeepAlive, RunAtLoad, environment variables, stdout/stderr paths, and label placeholders.
- Make it suitable for per-user LaunchAgents by default.

12. `templates/macos/resource-budget.yml.j2`
- Provide a YAML template for CPU and memory budgets.
- Include placeholders for per-agent caps and optional overrides.

13. `templates/macos/local-services.yml.j2`
- Provide a YAML template describing localhost and tailnet service exposure.
- Include bind addresses, exposed ports, and reachability flags.

14. `templates/security/clawvisor-policy.yml.j2`
- Provide a policy template for Clawvisor.
- Include identity, access boundaries, allowed tools, approved registries, and enforcement actions.

15. `templates/security/acl.yml.j2`
- Provide a generic ACL template.
- Include subject, action, resource, and effect fields.

Schema file requirements:
- Each schema file must be valid JSON Schema draft-compatible syntax.
- Keep schemas minimal but useful.
- Include `type`, `properties`, and a small set of required fields.
- Reflect the corresponding registry/template use case.

Schemas to generate:

1. `files/schemas/acp-registry.schema.json`
- Schema for ACP registry entries.

2. `files/schemas/ans.schema.json`
- Schema for ANS identity and resolution records.

3. `files/schemas/agentregistry.schema.json`
- Schema for Agent Registry dashboard records.

4. `files/schemas/a2a-agent-card.schema.json`
- Schema for A2A Agent Card artifacts.

5. `files/schemas/mcp-server.schema.json`
- Schema for MCP server registry entries.

6. `files/schemas/telegram-agent.schema.json`
- Schema for Telegram bot/agent metadata.

Utility script requirements:

1. `files/scripts/validate-json.sh`
- Provide a safe shell script stub for validating JSON files.
- Include a shebang and a placeholder implementation using `jq` or Python if available.
- Make it non-destructive and easy to extend.

2. `files/scripts/health-check.sh`
- Provide a safe shell script stub for validating service health.
- Include a shebang and placeholder logic for HTTP, process, or TCP checks.

Implementation style:
- Keep file contents concise and practical.
- Use placeholders where real values are not yet known.
- Ensure templates are reusable across registry, runtime, and security roles.
- Make the JSON/YAML files valid and parseable.
- Keep shell scripts POSIX-friendly where possible.
- Use comment headers only when they improve readability.

Output requirements:
- Output each file in its own fenced code block.
- Precede each code block with the full file path.
- Do not output any additional files.
- Do not omit any file listed above.
- Keep the output directly usable as repository content.

Acceptance criteria:
- All shared templates are valid and reusable.
- All schema files are valid JSON Schema stubs.
- All utility scripts are safe, minimal, and ready for later extension.
- The repo can be bootstrapped end-to-end with these shared artifacts in place.

Now generate the shared `templates/` and `files/` scaffolding exactly as specified.
