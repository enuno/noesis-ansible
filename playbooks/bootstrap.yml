---
- name: Bootstrap hosts for NoesisPraxis
  hosts: all
  gather_facts: true
  tasks:
    - name: Ensure required directories exist
      ansible.builtin.file:
        path: "{{ item }}"
        state: directory
        mode: '0755'
      loop:
        - /opt/noesispraxis
        - /var/log/noesispraxis
