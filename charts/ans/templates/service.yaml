apiVersion: v1
kind: Service
metadata:
  name: {{ include "ans.fullname" . }}
  labels:
    {{- include "ans.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ans.selectorLabels" . | nindent 4 }}
