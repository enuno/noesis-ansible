apiVersion: v1
kind: Service
metadata:
  name: {{ include "mempalace.fullname" . }}
  labels:
    {{- include "mempalace.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mempalace.selectorLabels" . | nindent 4 }}
