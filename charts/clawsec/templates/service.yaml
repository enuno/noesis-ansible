apiVersion: v1
kind: Service
metadata:
  name: {{ include "clawsec.fullname" . }}
  labels:
    {{- include "clawsec.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "clawsec.selectorLabels" . | nindent 4 }}
