apiVersion: v1
kind: Service
metadata:
  name: {{ include "skillnet.fullname" . }}
  labels:
    {{- include "skillnet.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "skillnet.selectorLabels" . | nindent 4 }}
