apiVersion: v1
kind: Service
metadata:
  name: {{ include "acp-registry.fullname" . }}
  labels:
    {{- include "acp-registry.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "acp-registry.selectorLabels" . | nindent 4 }}
