apiVersion: v1
kind: Service
metadata:
  name: {{ include "telegram.fullname" . }}
  labels:
    {{- include "telegram.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "telegram.selectorLabels" . | nindent 4 }}
