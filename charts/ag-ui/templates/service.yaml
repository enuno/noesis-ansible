apiVersion: v1
kind: Service
metadata:
  name: {{ include "ag-ui.fullname" . }}
  labels:
    {{- include "ag-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ag-ui.selectorLabels" . | nindent 4 }}
