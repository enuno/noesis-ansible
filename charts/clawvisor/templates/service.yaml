apiVersion: v1
kind: Service
metadata:
  name: {{ include "clawvisor.fullname" . }}
  labels:
    {{- include "clawvisor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "clawvisor.selectorLabels" . | nindent 4 }}
