---
- name: Render SkillNet MCP configuration
  ansible.builtin.template:
    src: skillnet-mcp-config.json.j2
    dest: "{{ skillnet_mcp_install_dir }}/mcp-config.json"
    mode: '0644'
  notify: restart skillnet_mcp
