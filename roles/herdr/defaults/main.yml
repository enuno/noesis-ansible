---
herdr_install_script_url: "https://herdr.dev/install.sh"
herdr_config_dir: "{{ ansible_user_dir | default(ansible_env.HOME) }}/.config/herdr"
herdr_resume_agents_on_restore: true
herdr_manage_ssh_config: true
herdr_manifest_check: true
herdr_integrations:
  - claude
  - codex
