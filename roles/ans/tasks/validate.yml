---
- name: Validate ANS health
  ansible.builtin.uri:
    url: "http://localhost:{{ ans_port }}/health"
    method: GET
    status_code: 200
  register: ans_health
  retries: 5
  delay: 3
  until: ans_health.status == 200
