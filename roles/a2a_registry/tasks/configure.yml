---
- name: Deploy A2A registry docker-compose
  ansible.builtin.template:
    src: docker-compose.yml.j2
    dest: "{{ a2a_registry_data_dir }}/docker-compose.yml"
    mode: '0644'
  notify: restart_a2a_registry
