---
- name: Deploy ACP registry docker-compose
  ansible.builtin.template:
    src: docker-compose.yml.j2
    dest: "{{ acp_registry_data_dir }}/docker-compose.yml"
    mode: '0644'
  notify: restart_acp_registry
