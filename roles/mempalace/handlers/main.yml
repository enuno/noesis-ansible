---
- name: Restart MemPalace instances
  ansible.builtin.docker_container:
    name: "noesis-mempalace-{{ item.name }}"
    state: started
    restart: true
  loop: "{{ mempalace_agents }}"
  listen: restart mempalace
